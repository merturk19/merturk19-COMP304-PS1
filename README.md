# to execute p1a, compile the .c file and run with ./p1a (child process level) to see process ids, process levels and parent process ids

# to execute p2a and see results, compile and run with ./p1b & to create defunct processes and see them with ps -l command in 5 second sleep time 
